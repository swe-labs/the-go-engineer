## Scope
- Update FE.7 so the exercise actually uses the concepts introduced by FE.8 and FE.9
- Refresh the README, starter file, implementation, and tests together

## Affected Sections / Lesson IDs
- s03: FE.7, FE.8, FE.9

## Why
- After the FE reorder, FE.7 should consume first-class functions and closures rather than teaching the older pre-reorder version.
- Keeping FE.7 aligned with its prerequisites makes the sequence feel intentional instead of accidental.

## Acceptance
- FE.7's README and `main.go` exercise use function values and a closure-based rule
- tests cover the reordered teaching flow
- local verification passes for the lesson and the curriculum validator
