# Composition Track

This folder is part of the foundations `s04` path and is now validated as part of the same README contract as the rest of the foundations sections.

Recommended learner flow:

1. Finish `04-types-design`.
2. Run `CO.1` in `05-composition/05-composition-and-embedding/1-composition`.
3. Continue to `CO.2` embedding.
4. Finish with `CO.3` bank account project.

The public lesson surfaces live inside `05-composition/05-composition-and-embedding/`.
