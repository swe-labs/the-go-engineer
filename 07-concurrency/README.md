# 07 Concurrency

## Mission

This stage now opens with why concurrency exists, keeps the existing goroutine/context/time tracks, and adds sync primitives as a first-class track before advanced patterns.

## Suggested Learning Flow

1. Start with GC.0-GC.7 to ground the reason concurrency exists and how goroutines communicate.
2. Use SY.1-SY.6 to learn the synchronization tools that repair shared-state bugs.
3. Context, time, and concurrency-pattern tracks stay available as the broader runtime coordination path.

## Next Step

After this section, continue to `08-quality-test`.
